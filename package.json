{
  "name": "outset",
  "version": "0.0.0",
  "description": "CSS Starter Kit",
  "main": "index.js",
  "scripts": {
    "build": "node-sass --output-style expanded -o ./css ./scss/themes/*.scss",
    "watch": "node-sass --output-style expanded -w -r -o ./css ./scss/themes/*.scss ./css",
    "serve": "lite-server -c bs-config.json",
    "start": "npm run build && concurrently \"npm run watch\" \"npm run serve\""
  },
  "keywords": [
    "CSS"
  ],
  "author": "Christopher Wallis <christopher.j.wallis@gmail.com> (http://notoriousb1t.com)",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^3.5.0",
    "lite-server": "^2.3.0",
    "node-sass": "^4.5.3"
  }
}
